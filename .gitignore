# scripts/refresh.sh writes its catalog/thumbnails to $XDG_CACHE_HOME
# (~/.cache/omarchy-theme-gallery/), outside this repo entirely — see
# README's "How it's built" for why. Nothing generated lands in here.
